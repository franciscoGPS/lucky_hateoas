name: lucky_hateoas
version: 0.1.0

authors:
  - Grok 4.5
  - Francisco C. <fco.coma@gmail.com>

crystal: ">= 1.6.0, < 2.0.0"

description: |
  Lightweight HATEOAS / HAL / HAL-FORMS helpers for Lucky (Crystal).
  Add `_links`, URI templates, pagination, affordances and `application/hal+json`
  support using Lucky's type-safe route helpers.

license: MIT

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    version: ~> 1.5.0
